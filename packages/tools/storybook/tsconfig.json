{
    "extends": "../../../tsconfig.base.json",
    "compilerOptions": {
        "noEmit": true,
        "allowImportingTsExtensions": true
    },
    // `files` cascades to .storybook-*/tsconfig.json automatically.
    // `include` does NOT cascade — each .storybook-*/tsconfig.json must
    // declare its own `include` covering: its own dir, ../src/, ../shared/,
    // and any future package directories.
    "files": [
        "../../../declarations/scss.d.ts",
        "../../../declarations/vite-env.d.ts",
        "../../../declarations/svgr.d.ts",
        "../../../declarations/shims-vue.d.ts"
    ]
}
