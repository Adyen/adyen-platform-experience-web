{
    "name": "@integration-components/transactions",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/domains/transactions",
    "tags": ["scope:transactions", "type:domain"],
    "targets": {
        "types:check": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/transactions/tsconfig.json"
            },
            "outputs": [],
            "cache": true
        },
        "types:watch": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/transactions/tsconfig.json --watch"
            },
            "cache": false
        }
    }
}
