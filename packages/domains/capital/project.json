{
    "name": "@integration-components/capital",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/domains/capital",
    "tags": ["scope:capital", "type:domain"],
    "targets": {
        "types:check": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/capital/tsconfig.json"
            },
            "cache": true
        },
        "types:watch": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/capital/tsconfig.json --watch"
            },
            "cache": false
        }
    }
}
