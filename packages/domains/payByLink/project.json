{
    "name": "@integration-components/payByLink",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/domains/payByLink",
    "tags": ["scope:payByLink", "type:domain"],
    "targets": {
        "types:check": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/payByLink/tsconfig.json"
            },
            "outputs": [],
            "cache": true
        },
        "types:watch": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc --noEmit --project packages/domains/payByLink/tsconfig.json --watch"
            },
            "cache": false
        }
    }
}
