export enum PopoverContainerPosition {
    TOP = 'top',
    RIGHT = 'right',
    BOTTOM = 'bottom',
    LEFT = 'left',
}

// TODO: add more roles as we figure them out
export enum PopoverContainerAriaRole {
    TOOLTIP = 'tooltip',
    POPOVER = 'dialog',
}

export enum PopoverContainerSize {
    SMALL = 'small',
    LARGE = 'large',
}
