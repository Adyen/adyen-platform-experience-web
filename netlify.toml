[build]
  edge_functions = "netlify/edge-functions"

[context."chore/level-access-demo"]
    [context."chore/level-access-demo".build]
        command = "cd demo && pnpm install && pnpm run build"
        publish = ".demo"

[[edge_functions]]
  path = "/api/*"
  function = "proxy-requests"
