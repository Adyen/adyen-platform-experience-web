import { FunctionalComponent } from 'preact';
import type { GrantAccountDisplayProps } from './types';

export const GrantAccountDisplay: FunctionalComponent<GrantAccountDisplayProps> = ({ accountType, grant, onDisplayClose }) => {
    return null;
};
