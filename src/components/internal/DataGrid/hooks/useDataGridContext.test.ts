/**
 * @vitest-environment jsdom
 */
import { test } from 'vitest';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { useDataGridContext } from './useDataGridContext';

test('useDataGridContext', () => {
    // Write your tests here
});
