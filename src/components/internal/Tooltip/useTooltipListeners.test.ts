/**
 * @vitest-environment jsdom
 */
import { test } from 'vitest';
import { useTooltipListeners } from './useTooltipListeners.ts';

test('useTooltipListeners', () => {
    // Write your tests here
});
