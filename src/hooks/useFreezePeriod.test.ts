/**
 * @vitest-environment jsdom
 */
import { test } from 'vitest';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { useFreezePeriod } from './useFreezePeriod';

test('useFreezePeriod', () => {
    // Write your tests here
});
