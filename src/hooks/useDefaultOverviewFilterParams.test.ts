/**
 * @vitest-environment jsdom
 */
import { test } from 'vitest';
import useDefaultOverviewFilterParams from './useDefaultOverviewFilterParams.ts';

test('useDefaultOverviewFilterParams', () => {
    // Write your tests here
});
