{
    "git": {
        "commitMessage": "chore: release v${version}",
        "tagName": "v${version}",
        "pushArgs": ["--follow-tags", "--set-upstream origin bump/${version}"],
        "tag": false,
        "requireUpstream": false,
        "push": false,
    },
    "npm": {
        "publish": false
    },
    "plugins": {
        "@release-it/conventional-changelog": {
            "preset": "angular",
            "infile": "CHANGELOG.md",
            "config": "changelog.config.js"
        }
    },
    "hooks": {
        "before:git:release": "git checkout -b bump/${version}",
        "after:bump": "./scripts/update-version.sh ${version}"
    }
}
