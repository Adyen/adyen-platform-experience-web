{
    "git": {
        "commitMessage": "chore: release v${version}",
        "tagName": "v${version}",
        "pushArgs": ["--set-upstream", "origin"],
        "tag": false,
        "push": true,
        "pushRepo": "HEAD:bump/new-version"
    },
    "npm": {
        "publish": false
    },
    "plugins": {
        "@release-it/conventional-changelog": {
            "preset": "angular",
            "infile": "CHANGELOG.md",
            "config": "changelog.config.js"
        }
    },
    "hooks": {
        "after:bump": "./scripts/update-version.sh ${version}"
    }
}
